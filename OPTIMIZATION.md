# 浅草心情 - 项目优化规划

## 已完成优化（v1.1）

### 1. 审核模式开关
- **文件**: `cloudfunctions/chat/index.js`、`cloudfunctions/chat/ai.config.js`
- **功能**: 通过云数据库 `app_config` 集合的 `review_mode` 字段控制是否调用AI
- **审核模式下**: 关键词匹配本地话术回复，不调用AI、不提取记忆
- **开启方式**: 默认为审核模式，过审后在数据库设置 `{ key: "review_mode", enabled: false }`

### 2. AI回复打字机效果
- **文件**: `src/pages/index/index.tsx`
- **功能**: AI回复到达后逐字显示（40ms/字），提升沉浸感
- **细节**: 打字过程中禁用输入，完成后保存消息

### 3. 情绪引擎优化 - 减少误匹配
- **文件**: `src/utils/emotion-engine.ts`、`src/pages/index/index.tsx`
- **问题**: 原逻辑关键词匹配太宽泛，"工作很开心"也会返回职场压力安慰
- **方案**: 
  - 非正面情绪标签需同时包含「场景关键词 + 负面语气词」才触发本地匹配
  - 消息超20字符优先走AI
  - 情绪短语按钮点击走强制本地匹配（`getForceLocalReply`）

### 4. 错误处理与降级方案
- **文件**: `src/pages/index/index.tsx`
- **功能**: AI调用失败自动重试1次，仍失败则降级到本地话术回复
- **细节**: 8秒超时提示"网络有点慢"，避免用户焦虑

---

## 待实施优化

### 高优先级

#### 前端组件拆分重构
- **目标**: 将1100+行的 index.tsx 拆分为独立组件
- **新组件**: ChatBubble、InputBar、DailyQuoteModal、GuideModal、ThinkingIndicator
- **收益**: 可维护性大幅提升，便于后续迭代

#### 额度系统服务端校验
- **目标**: 在云函数中增加服务端额度校验
- **方案**: 云数据库 `user_quotas` 集合存储每日使用量
- **收益**: 防止客户端绕过额度限制

### 中优先级

#### 消息持久化到云端
- **目标**: 聊天记录同步到云数据库
- **方案**: `chat_messages` 集合，异步写入不阻塞
- **收益**: 换设备不丢失聊天记录

#### 用户留存 - 连续签到机制
- **目标**: 每日签到奖励额外对话次数
- **方案**: 连续3/7/14天分级奖励，数据存 `user_checkins`
- **收益**: 提升DAU和次日留存

#### 分享机制优化
- **目标**: 从一次性奖励改为每日分享奖励
- **方案**: 每天首次分享获5次额外额度
- **收益**: 持续裂变动力

### 低优先级

#### 语音输入功能
- 接入微信语音识别API，转文字后发送

#### 情绪日记/报告
- 基于 moodHistory 数据生成周/月情绪报告
- 可视化展示情绪变化趋势

#### 多角色/人设切换
- 支持多种陪伴风格（温柔姐姐/暖男/知心好友）
- 通过修改 System Prompt 实现

---

## 运营建议

### 审核合规策略
1. 小程序描述避免"AI"、"智能"、"深度合成"等字眼
2. 功能介绍写为"情绪陪伴工具"、"暖心话术"
3. 审核版本保持审核模式开启
4. 过审后通过数据库开关恢复AI功能

### 增长策略
1. 分享裂变：双向奖励机制已实现
2. 内容运营：每日治愈文案可结合热点更新
3. 社群引流：小红书/抖音发情感类短视频引流
4. 留存提升：签到+纪念日+情绪报告

### 商业化方向（后期）
1. 高级会员：无限对话次数 + 更优模型（DeepSeek）
2. 定制人设：付费解锁不同陪伴风格
3. 情绪报告：付费生成深度情绪分析报告
